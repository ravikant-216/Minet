server:
  port: 9005
spring:
  datasource:
    url: jdbc:h2:mem:db;db_close_delay=-1
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false
    out-of-order: true

